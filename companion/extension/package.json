{
  "name": "burner-companion-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@noble/hashes": "^1",
    "@scure/base": "^2",
    "@scure/bip32": "^2",
    "bip39": "^3.1.0"
  },
  "devDependencies": {
    "chrome-types": "^0.1.422",
    "esbuild": "^0.25",
    "typescript": "^5"
  }
}
